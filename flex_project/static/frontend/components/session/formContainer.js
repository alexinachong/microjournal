import { connect } from 'react-redux';
import Session from './form';

const mapStateToProps = (state, ownProps) => ({

});

const mapDispatchToProps = (state, ownProps) => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(Session);
